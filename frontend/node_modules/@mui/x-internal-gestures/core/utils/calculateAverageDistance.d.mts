import { PointerData } from "../PointerManager.mjs";
/**
 * Calculate the average distance between all pairs of pointers
 */
export declare function calculateAverageDistance(pointers: PointerData[]): number;